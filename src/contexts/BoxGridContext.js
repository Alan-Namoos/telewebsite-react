// import React, { useState, createContext } from "react";

// export const BoxGridContext = createContext();

// const BoxGridContextProvider = props => {
//   const [boxList, setBoxList] = useState([]);

//   const addBox = box => {
//     setBoxList([...boxList, box]);
//   };

//   return (
//     <BoxGridContext.Provider value={{ boxList, addBox }}>
//       {props.children}
//     </BoxGridContext.Provider>
//   );
// };

// export default BoxGridContextProvider;
